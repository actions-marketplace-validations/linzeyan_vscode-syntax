{
  "name": "poly-editor",
  "displayName": "Poly Editor",
  "description": "Editor-side conveniences with no CI counterpart: copy path:line references in the same shape poly's diagnostics print.",
  "version": "0.15.1",
  "publisher": "ricky",
  "license": "MIT",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./dist/extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "markdown.markdownItPlugins": true,
    "markdown.previewScripts": [
      "./dist/preview.js"
    ],
    "configuration": {
      "title": "Poly Editor",
      "properties": {
        "poly.indentTint.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Tint each level of leading whitespace, and mark whitespace that does not fill a level -- a mixed tab/space line shows up as a partial band rather than as nothing. Costs one set of decorations per visible line; nothing is scanned outside the viewport. Colours are the poly.indentLevel1-4 and poly.indentPartial theme colours, each overridable in workbench.colorCustomizations."
        },
        "poly.imagePreview.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Show a thumbnail in the gutter for lines that name an image file which exists. Only lines on screen are resolved, and only paths that resolve to a real file in the workspace; a URL or a missing file draws nothing. Costs one file read per distinct image."
        },
        "poly.referencesCodeLens.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Show a reference count above each declaration, an implementation count above each type and method -- `N impls` over an interface (who satisfies it) and `N interfaces` over a concrete type (what it satisfies) -- and `N methods` over a type whose methods are declared outside it, as Go's are. Every count comes from whatever reference provider the language already has; poly analyses nothing, so a language with no provider gets no lens. Costs one reference query per declaration scrolled into view. Clicking a count of one goes straight there; clicking a larger one opens the references list."
        },
        "poly.protobufCodeLens.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Link a .proto declaration to the Go protoc generated for it: `go type` over a message or enum, `go server` and `go client` over a service, and an implementation count over each rpc. The lens is drawn only when the generated file is already in the workspace and holds the declaration, so nothing points at a file that has not been generated yet. Requires poly.languageServers, because the .proto outline comes from `buf lsp serve`. Covers protoc-gen-go and protoc-gen-go-grpc; connect-go and other generators name their output in ways the proto does not predict and are left alone."
        },
        "poly.runCodeLens.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Show `run | debug` above a program's entry point -- `main` in Go, Rust, C, C++ and Java, `Main` in C#, `if __name__ == \"__main__\"` in Python, and the shebang of a shell script. `run` executes the file in an integrated terminal; `debug` hands over to whichever debug extension is installed for the language, exactly as F5 would. poly ships no debugger."
        },
        "poly.markdownMermaid.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Render ```mermaid fences as diagrams in the markdown preview. Ignored on VSCode 1.135 and later, which ships the same renderer as a built-in (mermaid-markdown-features): poly stands down rather than draw a second diagram over the first."
        },
        "poly.postfixCompletion.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Offer postfix templates after a dot: `err.if` expands to `if err != nil { }` in Go, `if (err) { }` in TypeScript, `if err:` in Python. Covers go, rust, swift, typescript, javascript (and their react variants), python, lua, c and cpp. These are text templates, not analysis -- poly reads the characters left of the dot and rearranges them -- and they sort after whatever the language server offered, so they never displace a real completion."
        },
        "poly.todo.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Show the TODOs view in the Explorer. The view scans the workspace for the markers in poly.todo.tags, and only while it is on screen -- a closed view costs nothing. Capped at 4000 files and 512KB per file, and it says so when it stops early."
        },
        "poly.todo.tags": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "TODO",
            "FIXME",
            "HACK",
            "XXX",
            "BUG"
          ],
          "description": "Markers the TODOs view lists. Matched as whole words, case-sensitively; entries that are not plain words are ignored."
        }
      }
    },
    "colors": [
      {
        "id": "poly.indentLevel1",
        "description": "Background of the first indent level.",
        "defaults": {
          "dark": "#FFFF4012",
          "light": "#FFFF4012",
          "highContrast": "#FFFF4020"
        }
      },
      {
        "id": "poly.indentLevel2",
        "description": "Background of the second indent level.",
        "defaults": {
          "dark": "#7FFF7F12",
          "light": "#7FFF7F12",
          "highContrast": "#7FFF7F20"
        }
      },
      {
        "id": "poly.indentLevel3",
        "description": "Background of the third indent level.",
        "defaults": {
          "dark": "#FF7FFF12",
          "light": "#FF7FFF12",
          "highContrast": "#FF7FFF20"
        }
      },
      {
        "id": "poly.indentLevel4",
        "description": "Background of the fourth indent level; levels beyond it cycle.",
        "defaults": {
          "dark": "#4FECEC12",
          "light": "#4FECEC12",
          "highContrast": "#4FECEC20"
        }
      },
      {
        "id": "poly.indentPartial",
        "description": "Background of leading whitespace that does not fill an indent level.",
        "defaults": {
          "dark": "#80202099",
          "light": "#80202066",
          "highContrast": "#802020CC"
        }
      }
    ],
    "commands": [
      {
        "command": "poly.copyPathWithLine",
        "title": "Copy Path with Line Numbers",
        "category": "Poly"
      },
      {
        "command": "poly.insertTableOfContents",
        "title": "Insert Table of Contents",
        "category": "Poly"
      },
      {
        "command": "poly.toggleBold",
        "title": "Toggle Bold",
        "category": "Poly"
      },
      {
        "command": "poly.toggleItalic",
        "title": "Toggle Italic",
        "category": "Poly"
      },
      {
        "command": "poly.refreshTodos",
        "title": "Refresh TODOs",
        "category": "Poly",
        "icon": "$(refresh)"
      },
      {
        "command": "poly.extractVariable",
        "title": "Extract Variable",
        "category": "Poly"
      },
      {
        "command": "poly.inlineVariable",
        "title": "Inline Variable",
        "category": "Poly"
      },
      {
        "command": "poly.moveToNewFile",
        "title": "Move to New File",
        "category": "Poly"
      },
      {
        "command": "poly.changeSignature",
        "title": "Change Signature",
        "category": "Poly"
      },
      {
        "command": "poly.implementInterface",
        "title": "Implement Interface",
        "category": "Poly"
      },
      {
        "command": "poly.nextChangedFile",
        "title": "Go to Next Changed File",
        "category": "Poly"
      },
      {
        "command": "poly.previousChangedFile",
        "title": "Go to Previous Changed File",
        "category": "Poly"
      },
      {
        "command": "poly.revertAndSave",
        "title": "Revert Selected Changes and Save",
        "category": "Poly"
      },
      {
        "command": "poly.continueList",
        "title": "Continue List",
        "category": "Poly"
      },
      {
        "command": "poly.indentListItem",
        "title": "Indent List Item",
        "category": "Poly"
      },
      {
        "command": "poly.outdentListItem",
        "title": "Outdent List Item",
        "category": "Poly"
      }
    ],
    "views": {
      "explorer": [
        {
          "id": "polyTodos",
          "name": "TODOs",
          "when": "config.poly.todo.enabled"
        }
      ]
    },
    "menus": {
      "editor/context": [
        {
          "command": "poly.copyPathWithLine",
          "group": "9_cutcopypaste"
        }
      ],
      "view/title": [
        {
          "command": "poly.refreshTodos",
          "when": "view == polyTodos",
          "group": "navigation"
        }
      ],
      "commandPalette": [
        {
          "command": "poly.refreshTodos",
          "when": "config.poly.todo.enabled"
        },
        {
          "command": "poly.continueList",
          "when": "false"
        },
        {
          "command": "poly.indentListItem",
          "when": "false"
        },
        {
          "command": "poly.outdentListItem",
          "when": "false"
        }
      ]
    },
    "keybindings": [
      {
        "command": "poly.toggleBold",
        "key": "ctrl+b",
        "mac": "cmd+b",
        "when": "editorTextFocus && !editorReadonly && editorLangId =~ /^(markdown|prompt|instructions|chatagent|skill)$/"
      },
      {
        "command": "poly.toggleItalic",
        "key": "ctrl+i",
        "mac": "cmd+i",
        "when": "editorTextFocus && !editorReadonly && editorLangId =~ /^(markdown|prompt|instructions|chatagent|skill)$/"
      },
      {
        "command": "poly.continueList",
        "key": "enter",
        "when": "editorTextFocus && !editorReadonly && !editorHasSelection && !editorHasMultipleSelections && !suggestWidgetVisible && !inSnippetMode && !inlineSuggestionVisible && editorLangId =~ /^(markdown|prompt|instructions|chatagent|skill|yaml)$/"
      },
      {
        "command": "poly.indentListItem",
        "key": "tab",
        "when": "editorTextFocus && !editorReadonly && !editorTabMovesFocus && !editorHasSelection && !editorHasMultipleSelections && !suggestWidgetVisible && !inSnippetMode && !inlineSuggestionVisible && editorLangId =~ /^(markdown|prompt|instructions|chatagent|skill)$/"
      },
      {
        "command": "poly.outdentListItem",
        "key": "shift+tab",
        "when": "editorTextFocus && !editorReadonly && !editorTabMovesFocus && !editorHasSelection && !editorHasMultipleSelections && !suggestWidgetVisible && !inSnippetMode && !inlineSuggestionVisible && editorLangId =~ /^(markdown|prompt|instructions|chatagent|skill)$/"
      },
      {
        "command": "poly.nextChangedFile",
        "key": "ctrl+alt+z",
        "mac": "cmd+alt+z",
        "when": "config.git.enabled && !git.missing"
      },
      {
        "command": "poly.previousChangedFile",
        "key": "ctrl+alt+a",
        "mac": "cmd+alt+a",
        "when": "config.git.enabled && !git.missing"
      },
      {
        "command": "poly.revertAndSave",
        "key": "alt+q",
        "when": "editorTextFocus && !editorReadonly && config.git.enabled && !git.missing && resourceScheme == file"
      },
      {
        "command": "poly.extractVariable",
        "key": "ctrl+alt+v",
        "mac": "cmd+alt+v",
        "when": "editorTextFocus && !editorReadonly"
      },
      {
        "command": "poly.inlineVariable",
        "key": "ctrl+alt+shift+v",
        "mac": "cmd+alt+shift+v",
        "when": "editorTextFocus && !editorReadonly"
      }
    ]
  },
  "scripts": {
    "build": "esbuild src/extension.ts --bundle --outfile=dist/extension.js --external:vscode --format=cjs --platform=node --minify && esbuild src/preview/mermaid.ts --bundle --outfile=dist/preview.js --format=iife --platform=browser --minify",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.preview.json --noEmit",
    "test": "tsc -p tsconfig.test.json && node --test out/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/vscode": "^1.85.0",
    "esbuild": "^0.28.1",
    "typescript": "^5.4.0"
  },
  "packageManager": "pnpm@11.5.3",
  "dependencies": {
    "@mermaid-js/layout-tidy-tree": "^1.0.0",
    "mermaid": "^11.17.2"
  }
}
