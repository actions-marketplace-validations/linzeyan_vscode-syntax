# Template syntax

Handlebars writes a variable as {{ name }}, and Jinja writes {{ user.email }}.

An inline code span is not special to an injection: `{{ escaped }}`.

    {{ an indented code block }}

- A list item mentioning {{ interpolation }} in passing.
